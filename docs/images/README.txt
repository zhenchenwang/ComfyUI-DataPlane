The live workflow screenshot is stored as `dataplane-governed-query.webp.base64.txt` because the current GitHub connector only supports UTF-8 file writes. Decode it locally with:

```bash
base64 -d docs/images/dataplane-governed-query.webp.base64.txt > docs/images/dataplane-governed-query.webp
```

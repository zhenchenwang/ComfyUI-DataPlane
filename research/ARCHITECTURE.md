# Architecture

The interface-first release separates profile resolution, policy evaluation, database adapters, structured node types and provenance.

Planned next layers are PostgreSQL, SQL Server, MySQL and Oracle adapters; cursor/checkpoint objects; queue workers; vector retrieval; approval gates; and resumable batch state.
